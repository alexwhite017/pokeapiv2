---
phase: 03-detail-page
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PokemonDetailsComponents/StatGraph.jsx
  - src/data/statColors.js
autonomous: true
requirements: [DETAIL-02, DETAIL-03]

must_haves:
  truths:
    - "Stat bars use value-based color thresholds: red for stats below 30, orange 30-59, yellow 60-89, lime 90-119, green 120-149, teal 150+"
    - "Each stat bar row has adequate vertical spacing and the bar itself has consistent height with rounded corners"
    - "Stat labels and numeric values are clearly readable in each row"
    - "Bars animate from 0% to their value width on mount with ~300ms ease-out transition"
    - "The bar track (empty portion behind the bar) shows bg-black/20 so the page gradient is visible through it"
  artifacts:
    - path: "src/data/statColors.js"
      provides: "Value-based color threshold function getStatBarColor(value) returning Tailwind bg color class"
    - path: "src/components/PokemonDetailsComponents/StatGraph.jsx"
      provides: "Polished stat bars with threshold colors, animation, rounded corners, row spacing, inline style widths"
  key_links:
    - from: "src/components/PokemonDetailsComponents/StatGraph.jsx"
      to: "src/data/statColors.js"
      via: "import getStatBarColor"
      pattern: "getStatBarColor"
    - from: "StatGraph stat bar div"
      to: "stat.base_stat value"
      via: "width inline style + getStatBarColor(stat.base_stat)"
      pattern: "getStatBarColor.*base_stat"
---

<objective>
Overhaul the StatGraph component to display quality-based color-coded stat bars with animation, consistent height, rounded corners, and clear labels.

Purpose: DETAIL-02 (threshold colors) and DETAIL-03 (polished presentation) — stat bars should communicate quality at a glance and be easy to scan.
Output: Updated statColors.js (value-based threshold function), updated StatGraph.jsx (animation, rounded bars, row spacing, threshold colors, transparent track).
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-detail-page/03-CONTEXT.md

<interfaces>
<!-- Key existing code. Use these directly. -->

From src/data/statColors.js (CURRENT — stat-name based, being replaced):
```js
export const statColors = {
  hp: "bg-green-500",
  attack: "bg-yellow-500",
  // etc.
};
```

From src/data/statBackground.js (CURRENT — used for row backgrounds, leave unchanged):
```js
export const statBackground = {
  hp: "bg-green-400",
  attack: "bg-yellow-400",
  // etc.
};
```

From src/components/PokemonDetailsComponents/StatGraph.jsx (CURRENT structure):
```jsx
import { statColors } from "../../data/statColors";
import { statBackground } from "../../data/statBackground";
import { statNames } from "../../data/statNames";

const StatGraph = ({ poke }) => {
  const type = poke.types[0].type.name;
  return (
    <div className={`stats w-full border-border-${type} bg-${type} border-1 rounded-xl ...`}>
      <div className={`bg-${type}-secondary m-1 rounded-xl`}>
        <div className={`header border-b-1 border-${type} ...`}>
          <h2>Stats</h2>
        </div>
        <div className={`bg-${type}-secondary`}>
          {poke.stats.map((stat, index) => (
            <div key={index} className={`flex h-6 border-${type} border-y-1 ${statBackground[stat.stat.name]}`}>
              <div className={`statName flex flex-1.5 sm:flex-1 w-[35%] border-${type} border-r-2 px-2 text-text-primary`}>
                <div className="flex-2 justify-start font-bold text-sm">{statNames[stat.stat.name]}:</div>
                <div className="flex-1 text-right text-sm"><p>{stat.base_stat}</p></div>
              </div>
              <div className="flex-3">
                <div
                  className={`${statColors[stat.stat.name]} my-[1px] border-1 rounded border-surface-border ml-0.5 h-5`}
                  style={{ width: `${((stat.base_stat / 255) * 100).toFixed(0)}%` }}
                />
              </div>
            </div>
          ))}
        </div>
        <!-- Total row at bottom -->
      </div>
    </div>
  );
};
```

Decision from 03-CONTEXT.md:
  - Bar color thresholds (value-based, not stat-name based):
      < 30   → red (e.g. bg-red-500)
      30-59  → orange (e.g. bg-orange-500)
      60-89  → yellow (e.g. bg-yellow-400)
      90-119 → lime (e.g. bg-lime-500)
      120-149→ green (e.g. bg-green-500)
      150+   → teal (e.g. bg-teal-400)
  - Animation: width transitions 0% → value% on mount, ~300ms ease-out
  - Bar track: bg-black/20 (semi-transparent) — gradient shows through
  - Bar height and row spacing: Claude's discretion
  - statBackground (row background) is no longer relevant to the bar colors — the stat row backgrounds can be removed or kept neutral

Decision from STATE.md:
  - Inline style for dynamic widths (established in Phase 1): use style={{ width: '...' }} not Tailwind JIT template literals
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace statColors.js with value-based threshold function</name>
  <files>src/data/statColors.js</files>
  <action>
    Replace the entire contents of statColors.js with a single exported function `getStatBarColor(value)` that returns the appropriate Tailwind bg color class based on the stat value threshold:

    ```js
    // Threshold-based stat bar color
    // < 30:    red    (very low)
    // 30-59:   orange (below average)
    // 60-89:   yellow (average)
    // 90-119:  lime   (good)
    // 120-149: green  (great)
    // 150+:    teal   (exceptional)
    export function getStatBarColor(value) {
      if (value < 30)  return "bg-red-500";
      if (value < 60)  return "bg-orange-500";
      if (value < 90)  return "bg-yellow-400";
      if (value < 120) return "bg-lime-500";
      if (value < 150) return "bg-green-500";
      return "bg-teal-400";
    }
    ```

    NOTE: The old `statColors` named export and `statBackground` import in StatGraph.jsx will be updated in Task 2. BasicData.jsx also imports statColors for EV display — check BasicData.jsx and if it uses the object form `statColors[statInfo.stat.name]`, leave BasicData.jsx as-is (it's outside this plan's scope) and keep a backwards-compat named export: `export const statColors = { hp: "bg-green-500", attack: "bg-yellow-500", defense: "bg-orange-500", speed: "bg-purple-500", "special-attack": "bg-blue-300", "special-defense": "bg-blue-500" }` in addition to the new function. This way BasicData.jsx continues to compile without changes.
  </action>
  <verify>File exists at src/data/statColors.js and exports both `getStatBarColor` function and `statColors` object. `npm run build` exits 0.</verify>
  <done>statColors.js exports getStatBarColor(value) returning correct threshold classes; backwards-compat statColors object preserved for BasicData.jsx; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul StatGraph.jsx with threshold colors, animation, and polished layout</name>
  <files>src/components/PokemonDetailsComponents/StatGraph.jsx</files>
  <action>
    Rewrite StatGraph.jsx to use the new getStatBarColor function, add mount animation, clean up row layout, and use a transparent bar track.

    **Imports:** Add `useState` and `useEffect` from react. Change statColors import to `import { getStatBarColor } from "../../data/statColors"`. Remove the `statBackground` import entirely (no longer using row background colors).

    **Animation state:** Add `const [animated, setAnimated] = useState(false)`. In a `useEffect(() => { const timer = setTimeout(() => setAnimated(true), 50); return () => clearTimeout(timer); }, [])`, trigger animation shortly after mount so CSS transition plays from 0% to the real width.

    **Row layout changes:**
    - Remove `${statBackground[stat.stat.name]}` from the row div className (removes per-stat row color)
    - Row div: give it `py-2` for vertical spacing (instead of fixed `h-6`). Keep `flex items-center`.
    - Label section: keep the stat name and value display, use `w-[30%]` with `text-text-primary text-sm font-semibold` for label, `text-right text-sm text-text-secondary` for value number
    - Track div: replace `flex-3` wrapper with a `flex-1` div that has `bg-black/20 rounded-full h-3` (the track background — semi-transparent dark so gradient shows through)
    - Bar div inside track: use `getStatBarColor(stat.base_stat)` for the color class, `rounded-full h-3` for shape, and inline style for width with transition: `style={{ width: animated ? \`${((stat.base_stat / 255) * 100).toFixed(0)}%\` : '0%', transition: 'width 300ms ease-out' }}`

    **Remove:** `border-y-1`, `border-${type}` from individual rows — keep the outer container border but not per-row borders (cleaner look).

    **Keep unchanged:** The outer `bg-${type}` container, `bg-${type}-secondary` inner section, header "Stats" title, Total row at the bottom.

    **Result structure per stat row:**
    ```jsx
    <div key={index} className="flex items-center py-2 px-2 gap-3">
      <div className="w-[30%] flex justify-between text-text-primary text-sm">
        <span className="font-semibold">{statNames[stat.stat.name]}</span>
        <span className="text-text-secondary">{stat.base_stat}</span>
      </div>
      <div className="flex-1 bg-black/20 rounded-full h-3">
        <div
          className={`${getStatBarColor(stat.base_stat)} rounded-full h-3`}
          style={{ width: animated ? `${((stat.base_stat / 255) * 100).toFixed(0)}%` : '0%', transition: 'width 300ms ease-out' }}
        />
      </div>
    </div>
    ```
  </action>
  <verify>Navigate to any Pokémon detail page. Stat bars animate in on page load. Bar colors reflect quality (low stats red/orange, high stats lime/teal). Bars have rounded ends. `npm run build` exits 0.</verify>
  <done>StatGraph uses value-based threshold colors; bars animate 0%→value on mount; rounded-full bars with transparent track; adequate row spacing; build clean</done>
</task>

</tasks>

<verification>
1. Visit /details/magikarp — Splash has 40 Attack, should be orange. Speed 80 → yellow.
2. Visit /details/mewtwo — many stats 110-154 range, should show lime/green/teal bars
3. Visit /details/shedinja — HP 1, should be red bar
4. Bars animate smoothly from left on page load (not instant)
5. `npm run build` exits 0
6. BasicData.jsx EVs section still renders without error (statColors object still exported)
</verification>

<success_criteria>
- DETAIL-02 satisfied: threshold color scheme applied (red/orange/yellow/lime/green/teal by value)
- DETAIL-03 satisfied: rounded bars, consistent height (h-3), clear labels + numeric values, py-2 row spacing
- Animation plays on mount (~300ms ease-out from 0%)
- Bar track is semi-transparent (bg-black/20)
- Build clean, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-page/03-02-SUMMARY.md`
</output>
