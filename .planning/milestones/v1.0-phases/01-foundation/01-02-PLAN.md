---
wave: 2
depends_on:
  - PLAN-A.md
files_modified:
  - src/components/NavBar.jsx
  - src/components/PokemonDetails.jsx
  - src/components/containerSkeleton.jsx
  - src/components/Results.jsx
  - src/components/PokemonDetailsComponents/BasicData.jsx
  - src/components/PokemonDetailsComponents/StatGraph.jsx
  - src/components/PokemonDetailsComponents/DexEntries.jsx
  - src/components/PokemonDetailsComponents/LearnSet.jsx
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
---

# Plan B: Component Dark Sweep & Stat Bar Fix

## Objective

Replace all hardcoded light-mode colors (`bg-white`, `text-black`, `bg-gray-*`, `text-gray-*`) across all components with the dark surface/text tokens defined in Plan A. Also fix the silent production build bug in `StatGraph.jsx` where runtime-interpolated Tailwind width classes fail in `vite build`. After this plan, no white islands remain on the dark page and stat bars render correctly in production.

## Context

- @src/components/NavBar.jsx
- @src/components/PokemonDetails.jsx
- @src/components/containerSkeleton.jsx
- @src/components/Results.jsx
- @src/components/PokemonDetailsComponents/BasicData.jsx
- @src/components/PokemonDetailsComponents/StatGraph.jsx
- @src/components/PokemonDetailsComponents/DexEntries.jsx
- @src/components/PokemonDetailsComponents/LearnSet.jsx

Tokens available after Plan A:
- `bg-surface-raised` → dark panel/card backgrounds (replaces `bg-white`, `bg-gray-300`)
- `bg-surface-inset` → recessed areas (replaces `bg-gray-500`)
- `text-text-primary` → main text (replaces `text-black`, `text-gray-800`)
- `text-text-secondary` → subdued labels (replaces `text-gray-600`)
- `border-surface-border` → subtle dividers (replaces `border-gray-500`)

## Tasks

### Task 1: Replace hardcoded light colors in all components (FOUND-02)

<files>
src/components/NavBar.jsx
src/components/PokemonDetails.jsx
src/components/containerSkeleton.jsx
src/components/Results.jsx
src/components/PokemonDetailsComponents/BasicData.jsx
src/components/PokemonDetailsComponents/DexEntries.jsx
src/components/PokemonDetailsComponents/LearnSet.jsx
</files>

<action>
Read each file listed below and apply the following substitution map to ALL className strings (including inside ternaries, template literals, and conditional expressions):

| Replace | With | Notes |
|---------|------|-------|
| `bg-white` | `bg-surface-raised` | Dark panel surface |
| `text-black` | `text-text-primary` | Off-white readable text |
| `bg-gray-300` | `bg-surface-raised` | NavBar header |
| `text-gray-800` | `text-text-primary` | NavBar text |
| `text-gray-600` | `text-text-secondary` | Subdued labels in Results |
| `bg-gray-500` | `bg-surface-inset` | DexEntries recessed areas |
| `border-gray-500` | `border-surface-border` | DexEntries borders |
| `hover:bg-green-400` | `hover:bg-surface-inset` | DexEntries tab hover (green is type-agnostic and incorrect here) |

**Files and known locations:**
- `NavBar.jsx`: `bg-gray-300 text-gray-800` on the outer nav div
- `PokemonDetails.jsx`: `bg-white`, `text-black` around lines 40, 49, 52, 53
- `containerSkeleton.jsx`: `text-black` around line 7
- `Results.jsx`: `text-gray-600` around lines 81, 84
- `BasicData.jsx`: 20+ instances of `bg-white`/`text-black` — replace ALL
- `DexEntries.jsx`: `bg-white`, `text-black`, `bg-gray-500`, `border-gray-500`, and the ternary tab active state `activeTab === index ? "bg-white" : "hover:bg-green-400"` — replace both sides: `"bg-surface-raised"` and `"hover:bg-surface-inset"`
- `LearnSet.jsx`: `bg-white`, `text-black`

After editing all files, run the following to confirm no missed instances:
```bash
grep -rn "bg-white\|text-black\|bg-gray-300\|bg-gray-500\|text-gray-800\|text-gray-600\|hover:bg-green-400" src/components/
```
Any remaining hits must be intentional (document inline if so). An empty result is the target.

Do NOT use `dark:` variants. Do NOT remove or modify any layout, spacing, or non-color classes. Do NOT touch `StatGraph.jsx` in this task (handled in Task 2).
</action>

<verify>
  <automated>grep -rn "bg-white\|text-black\|bg-gray-300\|bg-gray-500\|text-gray-800\|text-gray-600\|hover:bg-green-400" src/components/</automated>
</verify>

<done>
Zero unintentional matches for `bg-white`, `text-black`, `bg-gray-300`, `bg-gray-500`, `text-gray-800`, `text-gray-600`, or `hover:bg-green-400` remain in `src/components/`. All replaced with dark surface/text tokens.
</done>

---

### Task 2: Fix stat bar production build bug in `StatGraph.jsx` (FOUND-04)

<files>
src/components/PokemonDetailsComponents/StatGraph.jsx
</files>

<action>
Open `src/components/PokemonDetailsComponents/StatGraph.jsx`. Find the stat bar div around line 41 that uses a runtime-interpolated width class:

```jsx
className={`... h-5 w-[${((stat.base_stat / 255) * 100).toFixed(0)}%]`}
```

Replace it with an inline `style` prop for the width. The change is MINIMAL — only remove the `w-[...]` portion from the className template literal and add `style={{ width: ... }}`. Do not change any other classes on that element or any surrounding elements.

```jsx
<div
  className="my-[1px] border-1 rounded border-gray-500 ml-0.5 h-5"
  style={{ width: `${((stat.base_stat / 255) * 100).toFixed(0)}%` }}
/>
```

Note: `border-gray-500` on this element will be swept as part of this task — replace it with `border-surface-border` to stay consistent with Task 1:

```jsx
<div
  className="my-[1px] border-1 rounded border-surface-border ml-0.5 h-5"
  style={{ width: `${((stat.base_stat / 255) * 100).toFixed(0)}%` }}
/>
```

Also apply Task 1's substitution map to any other classes in `StatGraph.jsx` (known: `text-black` around lines 16, 27, 54 — replace with `text-text-primary`).

Do NOT change the stat calculation logic (`stat.base_stat / 255 * 100`). Do NOT modify the surrounding flex/grid layout. Make only the class replacement and style prop addition.
</action>

<verify>
  <automated>grep -n "w-\[" src/components/PokemonDetailsComponents/StatGraph.jsx</automated>
</verify>

<done>
No `w-[${...}%]` runtime interpolation remains in `StatGraph.jsx`. The stat bar div uses `style={{ width: '...' }}` instead. `text-black` replaced with `text-text-primary`.
</done>

---

## Verification

Run the full production build and preview to confirm stat bars work and no white surfaces remain:

```bash
npm run build && npm run preview
```

In the preview (production build):
1. Navigate to any Pokémon detail page — stat bars must vary in width (not all 0% or all 100%)
2. At viewport width > 1280px — no white side-strips visible
3. No white panels or white text backgrounds anywhere on the page

## must_haves

- Zero `bg-white` or `text-black` instances remain in `src/components/` (grep returns empty)
- Zero `hover:bg-green-400` remains in any component
- `StatGraph.jsx` stat bar uses `style={{ width: '...' }}` — no `w-[${...}%]` template interpolation
- `npm run build && npm run preview` succeeds and stat bars have varied widths on a Pokémon detail page
- All layout/spacing/non-color classes are unchanged across all modified files
